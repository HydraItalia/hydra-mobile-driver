# Hydra Driver (Expo RN) — MVP Skeleton

API base URL (dev): https://hydra-sable-tau.vercel.app

Goal (MVP):
- Simple app shell with navigation
- A Ping screen to verify network connectivity to the API base URL
- Basic folder structure: src/config, src/api, src/screens, src/navigation
- Axios client
- No auth yet (just ping)

Tech:
- Expo + React Native + TypeScript
- Axios
- Minimal UI (RN components)
